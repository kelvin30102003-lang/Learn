import Link from "next/link";
import { questions } from "@/data/mock";
export default async function ReviewPage({ params }: { params: Promise<{ attemptId: string }> }) { await params; return <div className="mx-auto max-w-4xl px-5 py-12"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[var(--red)]">Answer review</p><h1 className="mt-3 text-4xl font-semibold">Learn from every question.</h1></div><Link href="/results/demo" className="text-sm text-[var(--red)]">← Back to result</Link></div><div className="mt-10 space-y-4">{questions.map((question, index) => { const answer = question.choices[Number(question.correctChoiceId) - 1]; return <article key={question.id} className="border border-[var(--line)] bg-white p-6"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-widest text-[var(--ink-muted)]">{question.section} · {index + 1}</p><span className="rounded-full bg-[var(--mint)] px-3 py-1 text-xs font-semibold text-[#317052]">CORRECT</span></div><h2 className="mt-5 text-lg font-medium leading-7">{question.questionText}</h2><div className="mt-5 grid gap-3 text-sm sm:grid-cols-2"><div className="border border-[#b8d4bf] bg-[var(--mint)] p-3"><span className="text-xs text-[#317052]">Correct answer</span><p className="mt-1 font-medium">{answer.key}. {answer.text}</p></div><div className="border border-[var(--line)] p-3"><span className="text-xs text-[var(--ink-muted)]">Your answer</span><p className="mt-1 font-medium">{answer.key}. {answer.text}</p></div></div><p className="mt-5 border-t border-[var(--line)] pt-4 text-sm leading-6 text-[var(--ink-muted)]"><strong className="text-[var(--foreground)]">Why:</strong> {question.explanation}</p></article>; })}</div></div>; }