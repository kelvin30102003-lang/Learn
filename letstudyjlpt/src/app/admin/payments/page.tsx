"use client";
import { useState } from "react";
const initial = [{ user: "Aung Min", method: "KPay", transaction: "KP-882193", date: "Today", status: "pending" }, { user: "Mika Chen", method: "WavePay", transaction: "WV-120490", date: "Yesterday", status: "pending" }];
export default function AdminPaymentsPage() { const [payments, setPayments] = useState(initial); const update = (index: number, status: string) => setPayments(payments.map((payment, i) => i === index ? {...payment, status} : payment)); return <div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[var(--red)]">Revenue</p><h1 className="mt-3 text-4xl font-semibold">Payment review</h1><p className="mt-3 text-[var(--ink-muted)]">Approve verified KPay and WavePay submissions to activate premium access.</p><div className="mt-8 overflow-hidden border border-[var(--line)] bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-[var(--line)] bg-[#f7f8f5] text-xs uppercase tracking-wider text-[var(--ink-muted)]"><tr>{["User", "Method", "Transaction", "Date", "Status", "Action"].map(label => <th key={label} className="px-5 py-4">{label}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{payments.map((payment, index) => <tr key={payment.transaction}><td className="px-5 py-4 font-medium">{payment.user}</td><td className="px-5 py-4">{payment.method}</td><td className="px-5 py-4 font-mono text-xs">{payment.transaction}</td><td className="px-5 py-4">{payment.date}</td><td className="px-5 py-4"><span className="rounded-full bg-[var(--cream)] px-3 py-1 text-xs font-semibold text-[#9a671f]">{payment.status.toUpperCase()}</span></td><td className="flex gap-2 px-5 py-4">{payment.status === "pending" && <><button onClick={() => update(index, "approved")} className="rounded-full bg-[#317052] px-3 py-2 text-xs font-semibold text-white">Approve</button><button onClick={() => update(index, "rejected")} className="rounded-full border border-[var(--line)] px-3 py-2 text-xs font-semibold text-[var(--red)]">Reject</button></>}</td></tr>)}</tbody></table></div></div></div>; }