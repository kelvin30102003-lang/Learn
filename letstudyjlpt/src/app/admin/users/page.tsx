import { adminClient } from "@/lib/custom-auth";

export default async function AdminUsersPage() {
  const { data: users } = await adminClient().from("users").select("id, full_name, email, target_level, role, is_premium, created_at").order("created_at", { ascending: false }).limit(100);
  return <div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[var(--red)]">People</p><h1 className="mt-3 text-4xl font-semibold">Users</h1><div className="mt-8 overflow-hidden border border-[var(--line)] bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-[var(--line)] bg-[#f7f8f5] text-xs uppercase tracking-wider text-[var(--ink-muted)]"><tr>{["User", "Email", "JLPT", "Role", "Plan", "Joined"].map((label) => <th key={label} className="px-5 py-4">{label}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{users?.length ? users.map((user) => <tr key={user.id}><td className="px-5 py-4 font-medium">{user.full_name}</td><td className="px-5 py-4">{user.email}</td><td className="px-5 py-4">{user.target_level}</td><td className="px-5 py-4 capitalize">{user.role}</td><td className={`px-5 py-4 ${user.is_premium ? "font-semibold text-[#317052]" : ""}`}>{user.is_premium ? "Premium" : "Free"}</td><td className="px-5 py-4">{new Date(user.created_at).toLocaleDateString()}</td></tr>) : <tr><td colSpan={6} className="px-5 py-8 text-center text-[var(--ink-muted)]">No users found.</td></tr>}</tbody></table></div></div></div>;
}
