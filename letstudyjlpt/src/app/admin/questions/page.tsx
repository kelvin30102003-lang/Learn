import { questions } from "@/data/mock";
export default function AdminQuestionsPage() { return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[var(--red)]">Content</p><h1 className="mt-3 text-4xl font-semibold">Questions</h1></div><button className="rounded-full bg-[var(--foreground)] px-5 py-3 text-sm font-medium text-white">+ New question</button></div><div className="mt-8 overflow-hidden border border-[var(--line)] bg-white"><div className="divide-y divide-[var(--line)]">{questions.map(question => <div key={question.id} className="flex items-start justify-between gap-4 p-5"><div><p className="text-xs font-semibold uppercase tracking-widest text-[var(--red)]">{question.section} · {question.questionNumber}</p><p className="mt-2 font-medium">{question.questionText}</p><p className="mt-1 text-sm text-[var(--ink-muted)]">{question.choices.length} choices · {question.questionType}</p></div><button className="text-sm text-[var(--red)]">Edit</button></div>)}</div></div></div>; }